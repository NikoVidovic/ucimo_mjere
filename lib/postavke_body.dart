import 'package:flutter/material.dart';

class PostavkeBody extends StatelessWidget {
  const PostavkeBody({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Prilagodite postavke'),
    );
  }
}
