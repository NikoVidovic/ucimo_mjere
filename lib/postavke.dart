import 'package:flutter/material.dart';
import 'postavke_body.dart';

class Postavke extends StatelessWidget {
  const Postavke({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: PostavkeBody());
  }
}
